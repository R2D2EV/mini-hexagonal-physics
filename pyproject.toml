[project]
name = "mini-hexagonal-physics"
version = "0.1.0"
description = "Tiny project for learning hexagonal architecture"
requires-python = ">=3.12"

dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
]

[dependency-groups]
dev = [
    "pytest>=8",
]